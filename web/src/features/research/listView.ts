import { ref } from 'vue'

import { DEFAULT_RESEARCH_LIST_VIEW, type ResearchListView } from '@/constants/lists'

// Режим раскладки списка исследований: таблица или плитки по группам. Читают его двое — страница
// со своим переключателем и сам список, поэтому состояние общее, а не локальное для вьюхи.
//
// Раньше это была настройка интерфейса (`interface_list_research_view`), общая на приложение и
// хранимая на сервере. Она ушла вместе с разделом исследований (2026-09-20): раздел снят с
// оболочки, и держать ради него ключ в реестре ядра было нечем оправдать. Здесь выбор живёт в
// памяти вкладки и перезагрузку не переживает — этого хватает, пока раздел не подключён.
export const researchListView = ref<ResearchListView>(DEFAULT_RESEARCH_LIST_VIEW)
