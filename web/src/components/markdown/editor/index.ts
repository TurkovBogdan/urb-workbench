// Публичный вход зоны правки.
//
// Наружу торчит компонент — и мост. Мост здесь не утечка внутренностей, а часть контракта:
// его круговой проход это единственный способ проверить, что редактор годен для настоящего
// тела, и страница дизайн-системы гоняет именно его. Всё остальное — схема, хром, поведение —
// подробности сборки, и импортировать их снаружи незачем.
export { default as MarkdownEditor } from './MarkdownEditor.vue'
export { docToMarkdown, markdownToDoc, UNSUPPORTED } from './bridge'
