// Чего мост не переносит — объявление, а не комментарий.
//
// Список отдельным файлом по двум причинам. Он выбрасывается НА ВХОДЕ, то есть расхождение
// видно в круговом проходе на витрине, а не однажды в сохранённом теле. И он же — единственный
// честный ответ на вопрос «можно ли наводить редактор на настоящее тело»: пока рендерер
// показывает конструкцию, которой здесь нет, правка через редактор её уничтожит.

export const UNSUPPORTED = [
  'images',
  'footnotes',
  'raw HTML',
  'blocks inside list items',
  // Не потеря смысла, а потеря написания: `` `TYPE@hash` `` становится пилюлей и печатается
  // обратно голым кодом. Читается он в ту же пилюлю, поэтому второй проход совпадает с первым —
  // но текст изменился, и умалчивать об этом список не вправе.
  'backticks around a whole code',
] as const
